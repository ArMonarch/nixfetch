name: release

# Only do the release on x.y.z tags.
on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"

# We need this to be able to create releases.
permissions:
  contents: write

jobs:
  # Build every target first. Each one is built natively on a runner of its own
  # architecture, so no QEMU emulation is involved. Nothing is published until
  # all of them succeed, so a broken build never leaves a stray draft release
  # behind.
  build-release:
    name: build-release (${{ matrix.name }})
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: x86_64-linux
            runner: ubuntu-latest
            package: .#packages.x86_64-linux.default
          - name: aarch64-linux
            runner: ubuntu-24.04-arm
            package: .#packages.aarch64-linux.default

    steps:
      - uses: actions/checkout@v4

      - uses: cachix/install-nix-action@v31

      - name: Build ${{ matrix.name }} binary
        run: nix build ${{ matrix.package }}

      - name: Copy binary
        run: cp result/bin/nixfetch nixfetch-${{ matrix.name }}

      - uses: actions/upload-artifact@v4
        with:
          name: nixfetch-${{ matrix.name }}
          path: nixfetch-${{ matrix.name }}

  # The create-release job runs purely to initialize the GitHub release itself,
  # and names the release after the `x.y.z` tag that was pushed. It's separate
  # from building the release so that we only create the release once.
  create-release:
    name: create-release
    needs: ['build-release']
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Get the release version from the tag
        if: env.VERSION == ''
        run: echo "VERSION=${{ github.ref_name }}" >> $GITHUB_ENV

      - name: Show the version
        run: echo "Version:" $VERSION

      - name: Create GitHub release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release create $VERSION --draft --verify-tag --title "nixfetch $VERSION"

    outputs:
      version: ${{ env.VERSION }}

  upload-release:
    name: upload-release
    needs: ['build-release', 'create-release']
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Upload release binaries
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: |
          version="${{ needs.create-release.outputs.version }}"
          gh release upload "$version" artifacts/*
